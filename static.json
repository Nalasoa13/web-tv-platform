{
  "root": "frontend/dist",
  "routes": {
    "/**": "index.html"
  },
  "proxies": {
    "/api/": {
      "origin": "${API_URL}"
    },
    "/socket.io": {
      "origin": "${API_URL}"
    },
    "/hls/": {
      "origin": "${HLS_URL}"
    }
  }
}
